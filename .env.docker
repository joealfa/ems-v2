# =============================================================================
# EMS-v2 Docker Environment Variables
# Copy this file and fill in your actual values before running docker compose.
# Usage: docker compose --env-file .env.docker up -d
# =============================================================================

# --- SQL Server (running on host) ---
DB_CONNECTION_STRING=Server=host.docker.internal,1433;Database=EmployeeManagementSystem;User Id=sa;Password=YourPassword;TrustServerCertificate=True;MultipleActiveResultSets=true

# --- JWT Authentication ---
JWT_SECRET=your-jwt-secret-key-min-32-chars-long
JWT_ISSUER=ems-api
JWT_AUDIENCE=ems-app

# --- Google OAuth ---
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# --- RabbitMQ (running on host) ---
RABBITMQ_HOST=host.docker.internal
RABBITMQ_PORT=5672
RABBITMQ_VIRTUAL_HOST=ems
RABBITMQ_USERNAME=your-rabbitmq-username
RABBITMQ_PASSWORD=your-rabbitmq-password

# --- Seq Logging (running on host) ---
SEQ_URL=http://host.docker.internal:5341
SEQ_API_KEY=your-seq-api-key

# --- Redis (running on host) ---
REDIS_CONNECTION_STRING=host.docker.internal:6379

# --- Frontend Build Args ---
VITE_GOOGLE_CLIENT_ID=your-google-client-id
